# Direkte Abhängigkeiten exakt gepinnt. Beim Anheben: Version hier ändern,
# Tests laufen lassen, committen — nicht per Untergrenze driften.
#
# requirements.txt beschreibt nur die direkten Abhängigkeiten. Docker und CI
# installieren requirements.lock, damit transitiven Updates Builds nicht
# unbemerkt verändern. Diese Datei bleibt der kurze, menschlich lesbare Ort
# für bewusste Upgrades.
fastapi==0.138.2
uvicorn[standard]==0.49.0
sqlalchemy==2.0.51
alembic==1.18.5
psycopg2-binary==2.9.10
pydantic-settings==2.14.2
PyJWT==2.13.0
bcrypt==5.0.0
webauthn==3.0.0

# Test-only Abhaengigkeiten stehen NICHT hier, sondern in
# requirements-dev.txt -- sonst landen sie ueber requirements.lock im
# Produktions-Image (siehe backend/Dockerfile, installiert nur requirements.lock).
