# Direkte Abhängigkeiten exakt gepinnt. Beim Anheben: Version hier ändern,
# Tests laufen lassen, committen — nicht per Untergrenze driften.
#
# Was hier NICHT steht: die transitiven Pakete (pydantic, starlette, anyio, …).
# Deren Versionen löst pip zur Bauzeit auf, zwei Builds an verschiedenen Tagen
# können sich also unterscheiden. Für dieses Projekt bewusst so gelassen —
# ein vollständiger Lockfile lohnt erst, wenn ein Build tatsächlich einmal an
# einem transitiven Update zerbricht. Dann: `uv pip compile requirements.txt -o
# requirements.lock` und Dockerfile/CI auf den Lockfile umstellen.
fastapi==0.138.2
uvicorn[standard]==0.49.0
sqlalchemy==2.0.51
alembic==1.18.5
psycopg2-binary==2.9.10
pydantic-settings==2.14.2
PyJWT==2.13.0
bcrypt==5.0.0
webauthn==3.0.0
pytest==9.1.1
httpx==0.28.1
